{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Studio window: core APIs (events, window) only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
