{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Studio + Display windows: core APIs (events, window) only.",
  "windows": ["main", "display"],
  "permissions": ["core:default"]
}
